<?php
/**
 * ═══════════════════════════════════════════════════════════════
 *  تنظیمات مرکزی — تنها فایلی که برای پیکربندی باید ویرایش کنی
 * ═══════════════════════════════════════════════════════════════
 *
 *  این فایل را به config.local.php کپی کن و مقادیر را پر کن.
 *
 *  بهترین جا برای config.local.php یک پله بیرون از پوشه‌ی عمومی
 *  سایت است (کنار public_html). برنامه خودش آن‌جا را هم می‌گردد.
 *  اگر میزبان اجازه نداد، همین کنار config.php هم کار می‌کند و
 *  فایل .htaccess جلوی خوانده شدنش را می‌گیرد.
 */

return [

    // ── هویت سایت ──────────────────────────────────────────────
    'site' => [
        'name'         => 'گفتگو هوشمند',
        'short_name'   => 'پیام‌رسان',
        'url'          => 'https://example.com',
        'theme_color'  => '#080e17',
        'default_room' => 'عمومی',
    ],

    // ── پایگاه داده ────────────────────────────────────────────
    'db' => [
        'host'    => 'localhost',
        'name'    => 'CHANGE_ME',
        'user'    => 'CHANGE_ME',
        'pass'    => 'CHANGE_ME',
        'charset' => 'utf8mb4',
    ],

    // ── امنیت ──────────────────────────────────────────────────
    'security' => [
        // کلید رمزنگاری پیام‌های خصوصی.
        // هشدار: بعد از اولین پیام هرگز عوضش نکن، پیام‌های قدیمی ناخوانا می‌شوند.
        'msg_encrypt_key' => '',

        // کلید مشترک با واسط. اگر واسطی نداری خالی بگذار.
        // خالی گذاشتن، امن‌ترین حالت است.
        'proxy_secret'    => '',

        'csrf_enabled'          => true,
        'session_lifetime_days' => 30,

        // اگر سایت پشت واسط یا شبکه‌ی توزیع محتواست، نام سرآیندی که
        // نشانی واقعی کاربر را می‌آورد این‌جا بنویس، مثلاً:
        // 'CF-Connecting-IP' یا 'X-Forwarded-For'. خالی یعنی نشانی مستقیم.
        'client_ip_header'      => '',
    ],

    // ── سقف‌ها و محدودیت‌ها ────────────────────────────────────
    'limits' => [
        'login_max_attempts'      => 10,   // تلاش ناموفق ورود
        'login_window_sec'        => 300,
        'login_lockout_sec'       => 900,

        'register_max_per_ip_day' => 3,    // حساب جدید از هر نشانی در شبانه‌روز
        'register_min_password'   => 8,
        'admin_set_min_password'  => 8,

        'max_file_size_mb'        => 10,
        'max_file_size_vip_mb'    => 25,
        'max_message_len'         => 2000,
    ],

    // ── امکانات ────────────────────────────────────────────────
    'features' => [
        'calls'        => true,   // تماس صوتی
        'registration' => true,   // ثبت‌نام باز (از پنل مدیریت هم قابل تغییر است)
    ],

    // ── نسخه‌ی فایل‌های ظاهری (برای تازه‌سازی حافظه‌ی مرورگر) ──
    'asset_version' => '2026-08-30-1',
];
